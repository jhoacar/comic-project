{
  "name": "comic-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install && cd ..",
    "start": "npm run start-server",
    "start-server": "npm start --prefix server",
    "build": "npm run install-all && npm run build-client && npm run build-server",
    "build-client": "npm run build --prefix client",
    "build-server": "./server/node_modules/typescript/bin/tsc",
    "dev": "concurrently \"npm:dev-*\"",
    "dev-client": "npm start --prefix client",
    "dev-server": "npm run dev --prefix server",
    "dev-watch-server": "npm run watch --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "concurrently": "^7.2.1"
  }
}
